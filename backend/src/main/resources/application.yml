server:
  port: 10025

spring:
  application:
    name: outpatient-registration
  
  datasource:
    url: jdbc:mysql://49.235.161.106:10011/db5?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: wl
    password: Wl1733811
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  
  data:
    redis:
      host: 49.235.161.106
      port: 10012
      database: 0
      timeout: 10000ms
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

jwt:
  secret: hospitalRegistrationSystemSecretKeyForJwtToken2024
  expiration: 86400000

logging:
  level:
    com.hospital: DEBUG
    org.hibernate.SQL: DEBUG
